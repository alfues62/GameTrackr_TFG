import { Dashboard } from "@/components/dashboard/Dashboard";

/**
 * "Mis juegos": el panel personal con estadísticas de la biblioteca
 * (KPIs, gráficas, top jugados), tus listas, recomendaciones y la
 * actividad reciente de tus amigos.
 */
export default function HomePage() {
  return <Dashboard />;
}
