[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py tests.py
testpaths = apps tests
# --ds fuerza los settings de test aunque el contenedor exporte
# DJANGO_SETTINGS_MODULE=config.settings.development (docker-compose).
addopts = -ra --ds=config.settings.test
